"""ME-3 Draw Audit: side-by-side baseline vs neuroim rewrite.

The mission gate.  Pick one realistic workflow, implement it twice (once
in raw nibabel+numpy, once through the public neuroim API), measure the
delta, and publish a report.  Both implementations consume the same
fixture from ``tests/fixtures/realistic_bold.py``.
"""
