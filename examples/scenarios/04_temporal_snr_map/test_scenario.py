"""Pointer test module for Scenario 04.

The runnable pytest lives one level up as
`examples/scenarios/test_s04_temporal_snr_map.py` because digit-prefixed
scenario folders are not importable Python packages under pytest's default
import mode.
"""
