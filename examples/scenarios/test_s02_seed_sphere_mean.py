"""Renumbered pointer module — see ``test_s03_seed_sphere_mean.py``.

The seed-sphere-mean scenario was renumbered from slot 02 to slot 03
after another agent landed ``02_roi_mean_timeseries`` in slot 02.
The runnable acceptance test now lives at
``examples/scenarios/test_s03_seed_sphere_mean.py``.

This file is kept (and contains no ``test_*`` functions) so pytest
collects nothing from it without erroring, and a directory listing of
``scenarios/`` makes the renumber visible.
"""
